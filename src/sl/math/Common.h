#pragma once

#include "Vector.hpp"

namespace sl::math {

const math::Vec3 worldUp = { 0.0f, 1.0f, 0.0f };
const math::Vec3 worldFront = { 0.0f, 0.0f, 1.0f };
const math::Vec3 worldRight = { 1.0f, 0.0f, 0.0f };
const math::Vec3 zerosVector = math::Vec3(0.0f);
const math::Vec3 onesVector = math::Vec3(1.0f);

}