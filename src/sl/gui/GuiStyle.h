#pragma once

#include "sl/math/Vector.hpp"

namespace sl::gui {

const sl::math::Vec4 guiDefaultTextColor = sl::math::asColor({230, 230, 230});
const sl::math::Vec4 selectedEntryColor = sl::math::asColor({255, 245, 110});
const sl::math::Vec4 disabledEntryColor = sl::math::asColor({99, 99, 99});

}  // namespace sl::gui